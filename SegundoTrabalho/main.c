#include <stdio.h>
#include <stdlib.h>

struct Node{
    int value;
    struct Node *next;
}

typedef struct Node Node;

void insertInList();
void deleteElement();
void initializeList();
void printList();

int main(int argc, char const *argv[])
{
    /* code */
    return 0;
}